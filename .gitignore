.DS_Store
.hugo_build.lock

# Hugo build output — regenerated by the GitHub Actions workflow on every deploy.
# Do not commit; the committed copy goes stale (points at localhost:1313).
/public/
/resources/_gen/